{
  "identifier": "about",
  "description": "Allow the independent About renderer to read the Switcher version, drag its shared titlebar, and use fixed project links.",
  "windows": [
    "about"
  ],
  "permissions": [
    "core:app:allow-version",
    "core:window:allow-start-dragging"
  ]
}
