# native/
> L2 | 父级: ../CLAUDE.md

成员清单
macos_permission_handoff.m: AppKit 权限交接 owner；以 WebView CSS source rect 与 viewport 为唯一几何输入，按屏幕切片生成非激活 panel，在真实 System Settings 窗口间复刻 blur、锁定阴影与 `1/200/11` 项目箭头的 shared-element 过渡；四语 helper 保持 Arrow→Instruction→App Row→Action 非重叠层级，随后提供 app-bundle file URL 真实拖拽、Retry/Cancel 与结果驱动 reverse/cleanup；只回报用户意图，不判断 TCC 是否已授权。

法则: 固定权限·非激活窗口·真实拖拽·业务结果裁决

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
