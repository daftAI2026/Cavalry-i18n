# assets/
> L2 | 父级: ../CLAUDE.md

成员清单
fonts/: renderer 的离线字体资产边界，固定 Geist Sans/Mono v1.7.2 与 OFL 许可；由 styles.css 直接消费，不依赖网络字体服务。

依赖方向:

assets 只向 renderer 提供静态资源，不承载交互、状态或平台逻辑；新增资源必须有可追溯来源、固定版本和许可。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
