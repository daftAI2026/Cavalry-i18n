# injector/
> L2 | 父级: /Users/luo/Desktop/ClaudeCode/web/Cavalry-i18n/desktop-patcher/CLAUDE.md

成员清单
generated_translations.inc: 历史桌面补丁器使用的编译期翻译表镜像。
libCavalryTranslatorInjector.dylib: 历史桌面补丁器使用的预编译 injector 动态库镜像。

依赖边界:
本目录只保留旧产物；新的翻译表生成与 dylib 构建以根目录 `injector/` 和 `tools/` 为准。

法则: 镜像不编辑·主线在根目录·历史不污染当前架构

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
