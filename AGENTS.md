# CAVALRY-I18N KNOWLEDGE BASE

Last verified: 2026-07-24 | Target: Cavalry 2.7.2 | Qt: 6.6.3 | Runtime: macOS + Windows x64

## OVERVIEW

Cavalry-i18n is a macOS and Windows x64 Tauri desktop language switcher for Cavalry. It patches keyed Cavalry JSON assets; macOS installs a DYLD runtime translator, patches Keychain query behavior, re-signs the local `Cavalry.app`, and clears quarantine; Windows deploys a Qt generic plugin to the selected installation root and launches Cavalry with child-only environment variables. It relaunches Cavalry in English, Simplified Chinese, Traditional Chinese, or Japanese.

Stack: vanilla HTML/CSS/JS renderer + Tauri 2 Rust backend + Objective-C++ Qt injector + Node/Python/Bash gate tooling. This is not a generic i18n repo; it is a local app-bundle patcher with runtime UI translation gates.

## STRUCTURE

```text
Cavalry-i18n/
├── renderer/                     # Tauri WebView UI; see renderer/CLAUDE.md
├── src-tauri/                    # Tauri v2 shell, Rust commands, tests, packaging config
│   ├── src/                      # Command layer, bundle patching, privilege boundary
│   └── tests/                    # Rust contract tests for command/config/runtime behavior
├── injector/                     # macOS Objective-C++ injector + Windows Qt generic plugin + shared generated table
├── languages/                    # Runtime JSON language packs: en, zh-Hans, zh-Hant, ja_JP
├── tools/                        # Build, extraction, translation, packaging, and gate scripts
├── docs/                         # Translation rules, runtime capture workflow, audits, gates
├── output/                       # Derived JSON-surface audit artifacts; not runtime truth
├── desktop-patcher/              # Legacy injector artifact mirror; not current mainline
├── .github/workflows/            # CI/CD, Windows NSIS / macOS package jobs, three-asset release publishing
├── LOCAL_BUILD_SOP.md            # Local Tauri packaging contract
├── release.config.json           # Cavalry target release tag/title/DMG asset truth source
└── CLAUDE.md                     # L1 architecture map; update when this map changes
```

## WHERE TO LOOK

| Task | Location | Notes |
| --- | --- | --- |
| Project map | `CLAUDE.md` + module `CLAUDE.md` | Read before touching a directory. L2/L3 docs are part of the contract. |
| Renderer UI | `renderer/index.html`, `renderer/app.js`, `renderer/styles.css` | Static DOM ids, localized shell text, custom select, modal, status panel. |
| Tauri bridge | `renderer/tauri-bridge.js`, `src-tauri/src/bridge.rs` | `window.cavalryI18n` is the only renderer API. Payloads are camelCase only. |
| IPC commands | `src-tauri/src/commands.rs` | Exactly 6 commands: status, browse, extract English, apply, open Privacy, restart. |
| JSON asset mapping | `src-tauri/src/patch.rs` | `CORE_MAP`, keyed overlay, packaged-English content proof, snapshot completeness/provenance. |
| System boundary | `src-tauri/src/privilege.rs` | Copy, admin fallback, re-signing, quarantine, Privacy & Security, restart commands. |
| Keychain patch | `src-tauri/src/keychain_patch.rs` | Mach-O/fat slice parser and NOP patcher for Keychain query attributes. |
| macOS runtime files | `src-tauri/src/mac_runtime.rs` | Launcher wrapper, Info.plist rewrite, language marker, injector copy pairs. |
| Windows runtime files | `src-tauri/src/windows_install.rs`, `windows_runtime.rs` | Discover arbitrary install roots, deploy generic plugin, child-only environment, marker readiness. |
| Runtime injectors | `injector/CavalryTranslatorInjector.mm`, `injector/windows/` | macOS first-paint translator; Windows Qt generic plugin with display whitelist and precise ExtensionLayer IAT boundary. |
| Embedded compiled translations | `tools/zh-Hans.ts`, `tools/zh-Hant.ts`, `tools/ja_JP.ts` | Qt Linguist XML sources for compiled/runtime UI strings. |
| Generated injector table | `injector/generated_translations.inc` | Generated by `tools/generate_embedded_translations.js`; never edit by hand. |
| Display-only model names | `tools/model_display_translations.json` | UI display translations for model names; do not write these into model identity data. |
| JSON language packs | `languages/en`, `languages/zh-Hans`, `languages/zh-Hant`, `languages/ja_JP` | `en` is baseline. Target languages must stay structurally isomorphic. |
| Translation policy | `docs/translation-guidelines.md`, `docs/cavalry-glossary.md`, `tools/translation-whitelist.json` | Field-level translate/no-translate boundaries and terminology. |
| Full UI gate | `docs/workflows/cavalry-full-ui-100/Acceptance.md`, `Runbook.md`, `tools/run_live_full_ui_matrix.js` | Current live gate truth. CI does not run this gate. |
| Runtime capture | `docs/runtime-ui-live-capture-workflow.md`, `tools/capture_accessibility_inventory.js`, `tools/merge_runtime_inventory.js` | Session-scoped provenance is mandatory. |
| Release protocol | `release.config.json`, `tools/release_metadata.js`, `.github/workflows/build.yml` | `cavalry-2.7.2-pN` tags drive release title and three assets: two DMGs plus Windows x64 NSIS EXE. |
| Local build | `LOCAL_BUILD_SOP.md`, `src-tauri/tauri.*.conf.json`, `tools/cavalry_qt_target.json` | Tauri-only package path, per-platform Qt build, DMG/NSIS validation. |

## CONVENTIONS

### Agent startup

- Start with `git status --short --branch`, `CLAUDE.md`, and the nearest module `CLAUDE.md`.
- For a business file, verify the L3 header contract before editing. If missing, add it first.
- Do not create a branch, commit, or push unless the user asks. This repo owner often expects direct `main` work.
- New root or module files require the matching `CLAUDE.md` map update in the same change.
- Communicate in Chinese, start user-facing replies with `哥`, and report evidence before claiming completion.

### Renderer

- `app.js` must not know Tauri internals. It talks only to `window.cavalryI18n`.
- `tauri-bridge.js` normalizes camelCase payloads only. Do not reintroduce snake_case fallbacks or debug fields.
- `index.html` ids are test anchors. Rename only with `tools/check_renderer_contract.js` and bridge/runtime tests updated.
- UI text is localized in `app.js`; translated Cavalry runtime UI belongs to `languages/` and `tools/*.ts`, not renderer shell copy.

### Tauri backend

- `commands.rs` is the renderer-facing API layer. Keep command count and JSON shape stable unless renderer and contract tests change together.
- `privilege.rs` is the only system-command boundary. Keep shell/admin/Finder fallback behavior there.
- `detect.rs`, `install.rs`, `patch.rs`, `mac_runtime.rs`, `windows_runtime.rs`, `keychain_patch.rs`, and `state.rs` should stay mostly pure data/filesystem logic.
- State may be redirected with `CAVALRY_I18N_STATE_DIR`; tests depend on this separation.
- `Status.version` is display-only. English snapshot invalidation uses the immutable bundle revision plus `EnglishSnapshotProvenance`; ordinary state sync must never manufacture snapshot provenance.
- English may be captured only after every `CORE_MAP` file passes packaged-English overlay equality. Missing markers require this proof; translated, pending, invalid, or Windows-empty markers fail closed.
- Language writes bracket assets/runtime with a `pending` marker and force the final language marker last, so an interrupted transaction cannot masquerade as clean English.

### Translation surfaces

- There are two real surfaces: JSON-backed assets and compiled/runtime Qt UI text.
- `languages/en` is the JSON baseline. Target language directories must match structure and preserve non-translatable identity fields.
- `niceName`, API/type/id fields, and Time Editor model-backed values stay English when they are model identity, even if display-layer UI translates them elsewhere.
- `tools/*.ts` handles compiled/runtime display text. `tools/model_display_translations.json` is display-only and must not rewrite JSON model data.
- Allowlist or whitelist changes require evidence. Do not use them to hide untranslated or fabricated output.

### Injector and Qt

- Both injector paths target Cavalry 2.7.2's Qt 6.6.3 runtime. Do not add Qt API symbols without ABI verification; Windows plugin deployment must not carry a second Qt runtime.
- `QMenu::aboutToShow`, `ActionAdded`, and `Show` are first-paint translation paths. Avoid delayed menu fixes that visibly flash English.
- `QLineEdit` display translation must not mutate model identity. Time Editor item views and `QAbstractItemView` model roles preserve English where required.
- `QTextEdit` MessageBar handling is append-time only. Do not scan full QTextEdit documents during Paint/Show or inventory.
- ExtensionLayer 只翻译经二进制调用点与真实界面共同证明的固定自绘 UI 提示；精确白名单之外的画布、模型及用户文本保持原文，禁止扩大为全局 `QPainter::drawText` 拦截。

### Build and release

- Tauri is the only active desktop shell. Do not restore old Electron/fallback packaging paths.
- Internal app version is SemVer in `CHANGELOG.md`, `package.json`, `src-tauri/Cargo.toml`, and `src-tauri/tauri.conf.json`; sync with `npm run sync:version`.
- Public release tags are `cavalry-2.7.2-pN`, generated against `release.config.json`.
- The platform injector must be built against Qt 6.6.3 via `npm run build:injector` (macOS) or `npm run build:injector:windows` (Windows) before its Tauri build.

## ANTI-PATTERNS

| Pattern | Why | Instead |
| --- | --- | --- |
| Treating CI green as full UI pass | CI records `BLOCKED-NO-LIVE-CAVALRY`; it cannot run Cavalry live capture | Run the local session-scoped full UI gate |
| Editing `generated_translations.inc` by hand | It is a generated C++ projection | Edit `tools/*.ts` or `model_display_translations.json`, then regenerate |
| Weak runtime inventory from cache root | Gate inputs must be session-scoped with provenance | Use `$SESSION_DIR/runtime/*` and `verify_gate_inputs.js` |
| Changing allowlists to pass coverage | Hides real translation debt | Fix source translations or add evidenced quarantine |
| Translating model `niceName` in JSON | Breaks Time Editor/model identity and may render CJK boxes | Keep model identity English; translate display layer |
| Adding raw Tauri invoke calls in `app.js` | Breaks renderer/bridge boundary | Extend `window.cavalryI18n` and tests together |
| Restoring old shell/build paths | Tauri-only path is the release contract | Use `LOCAL_BUILD_SOP.md` |
| Handwriting release title or DMG name | Drifts from tag protocol | Use `tools/release_metadata.js` |
| Editing real `/Applications/Cavalry.app` in tests | Tests must not mutate user apps by accident | Use fixtures or explicit manual smoke |
| Assuming `output/` is source truth | It is derived audit/draft material | Promote validated data into `languages/` |

## COMMANDS

```bash
# Setup
npm install

# Fast metadata and syntax checks
npm run check:version
npm run check:release
npm run check:app
npm run test:contracts

# Rust contracts
npm run check:tauri
npm run test:tauri

# Injector and Qt SDK
npm run prepare:qt-sdk
npm run prepare:qt-sdk:windows
npm run build:injector

# Local Tauri build / package
npm run tauri:dev
npm run tauri:build
npm run build:tauri
npm run build:tauri:windows
npm run test:tauri:packaged
npm run test:tauri:dmg-layout
npm run test:tauri:ui

# Full UI live gate, requires real Cavalry.app
npm run extract:compiled-ui
node tools/run_live_full_ui_matrix.js --app /Applications/Cavalry.app
# Reuse the session UUID printed by run_live_full_ui_matrix.js
SESSION_DIR="$HOME/Library/Caches/Cavalry-i18n/sessions/<session-uuid>" npm run check:full-ui
```

Manual smoke is intentionally ignored by default:

```bash
npm run test:tauri:manual-smoke
```

## BUILD PIPELINE

```text
languages/{lang}/ JSON assets
        +
tools/{zh-Hans,zh-Hant,ja_JP}.ts + model_display_translations.json
        ↓
tools/generate_embedded_translations.js
        ↓
injector/generated_translations.inc
        ↓
tools/build_translator_injector.sh  (macOS Qt 6.6.3)
        ↓
injector/libCavalryTranslatorInjector.dylib
        ↓
tauri.conf.json bundle resources: renderer + languages + injector
        ↓
Cavalry Language Switcher.app
        ↓
patch local Cavalry.app: JSON assets + wrapper + DYLD injector + re-sign + quarantine clear
        ↓
live runtime inventory + full UI coverage gate
```

Windows packaging follows the same JSON source and generated translation table, but deploys `injector/windows/generic/cavalryi18n.dll` into the selected Cavalry root, starts it with child-only plugin variables, and produces an NSIS installer. The Windows plugin/NSIS CI gate is not evidence of a live Cavalry UI pass.

## TOOLCHAIN

| Tool | Version / Source | Notes |
| --- | --- | --- |
| Node.js | 22 in CI | npm package root, no pnpm/yarn |
| npm packages | `package-lock.json` | `@tauri-apps/cli` and `@tauri-apps/api` pinned to 2.10.1 |
| Rust | stable, edition 2021 | Tauri backend crate `cavalry-i18n-tauri` |
| Tauri | Rust `tauri` 2.10.3 | npm CLI/API minor aligned with Rust runtime |
| `tauri-build` | 2.5.6 exact | Build script dependency |
| Qt SDK | 6.6.3 | Must match Cavalry 2.7.2 target runtime |
| Cavalry target | 2.7.2 | Release tag prefix `cavalry-2.7.2-p` |
| Python | python3 | Translation validator and `aqtinstall` setup |
| macOS tools | `codesign`, `hdiutil`, `xattr`, `osascript` | Required for package/sign/quarantine/runtime capture |
| Windows build tools | PowerShell 5.1, VS 2022 x64 Build Tools, CMake 3.21+, Qt 6.6.3 `msvc2019_64` | Developer build only; released NSIS users need none of Python/Rust/Qt/PowerShell 7 |

## SECURITY

- This app modifies a selected local Cavalry installation: macOS writes `Cavalry.app`, Windows writes keyed JSON assets and one generic plugin under the selected install root. macOS expects App Management permission; Windows uses direct writable-root copy or the restricted Program Files UAC path below.
- Windows direct copy may target an explicitly selected custom install root, but UAC fallback is restricted to canonical `FOLDERID_ProgramFiles`/`FOLDERID_ProgramFilesX86` roots from `SHGetKnownFolderPath`; never derive an elevation allowlist from process environment variables, and fail closed on any destination-chain reparse point. The elevated PowerShell recheck is defense in depth, not proof that every TOCTOU race is eliminated; the core boundary is the OS-known Program Files ACL.
- The release DMG is ad-hoc signed, not Apple Developer ID notarized. Do not claim notarization.
- `DYLD_INSERT_LIBRARIES` injection is intentional and limited to the patched Cavalry launcher wrapper.
- Keychain behavior is patched in `libExtensionLayer.dylib`; keep tests around per-function patch reports.
- Never commit real secrets, user caches, live session artifacts, or real Cavalry app bundles.
- `qt_sdk/`, `node_modules/`, `src-tauri/target/`, `src-tauri/gen/`, build outputs, and logs are ignored for a reason.

## NOTES

- `docs/workflows/cavalry-full-ui-100/` is the current gate system. The early `cavalry-i18n` workflow lives under `docs/archive/workflows-cavalry-i18n/` as historical evidence.
- `desktop-patcher/` is a legacy artifact mirror; the root `injector/` and Tauri bundle resources are the active path.
- `output/json-surfaces/` is audit/draft state. It can guide translation work, but runtime source truth is `languages/`.
- Several files are intentionally large because they encode contract coverage or translation surfaces. Do not casually split them during unrelated work.
- For any architecture change, update L3 headers, L2 module maps, and the L1 `CLAUDE.md` map before calling the task done.
